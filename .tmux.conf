#Set the default terminal to 256color mode
set -g default-terminal "screen-256color"


#Highlight active window
set-window-option -g window-status-current-bg 6

#Session config. Start tmux with 'tmux attach' to avoid the implicit new-session when you run 'tmux'
new-session -s SESSION1  
split-window -h
#select-pane -t 0
#split-window -v  
new-window 
split-window -h
select-pane -t 0
select-window -t 0
select-pane -t 0 


# default statusbar colors
#set-option -g status-bg white 
#set-option -g status-fg yellow 
#set-option -g status-attr default

# default window title colors
#set-window-option -g window-status-fg brightyellow 
#set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
#set-window-option -g window-status-current-fg brightred 
#set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
#set-option -g pane-border-fg white 
#set-option -g pane-active-border-fg brightcyan 

# message text
#set-option -g message-bg white 
#set-option -g message-fg brightred 

# pane number display
#set-option -g display-panes-active-colour blue 
#set-option -g display-panes-colour brightred 

# clock
#set-window-option -g clock-mode-colour green 
